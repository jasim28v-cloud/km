name: 🃏 Build APK - Joker

on:
  workflow_dispatch:
    inputs:
      app_name:
        description: '📱 اسم التطبيق'
        required: true
        default: 'جوكر'
        type: string
      app_id:
        description: '🆔 App ID (مثال: com.joker.app)'
        required: true
        default: 'com.joker.app'
        type: string
      app_color:
        description: '🎨 لون خلفية التطبيق (مثال: #FF0000)'
        required: true
        default: '#FF0000'
        type: string
      icon_file:
        description: '🖼️ اسم ملف الأيقونة PNG (ضع الملف في مجلد المشروع)'
        required: true
        default: 'icon.png'
        type: string
      site_folder:
        description: '📁 مجلد موقع الويب (اتركه فارغاً للمجلد الرئيسي)'
        required: false
        default: ''
        type: string
      splash_duration:
        description: '⏱️ مدة شاشة البداية (بالمللي ثانية)'
        required: false
        default: '3000'
        type: string

permissions:
  contents: write

jobs:
  build-apk:
    name: 📱 بناء تطبيق ${{ github.event.inputs.app_name }}
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 تحميل المشروع بالكامل
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: ☕ تثبيت Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🟢 تثبيت Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 تثبيت Capacitor والأدوات
        run: |
          npm install -g @capacitor/cli @capacitor/core @capacitor/android
          sudo apt-get update
          sudo apt-get install -y imagemagick

      - name: 📁 سحب وتجهيز جميع ملفات الموقع
        run: |
          echo "🔍 جاري سحب جميع ملفات المشروع..."
          
          # عرض جميع الملفات الموجودة في المشروع
          echo "📋 جميع الملفات في المشروع:"
          find . -type f -not -path './.git/*' -not -path './node_modules/*' | sort
          
          # عرض المجلدات
          echo ""
          echo "📂 المجلدات الموجودة:"
          find . -type d -not -path './.git/*' -not -path './node_modules/*' | sort
          
          # إنشاء مجلد www
          rm -rf www
          mkdir -p www
          
          # تحديد مجلد المصدر
          SOURCE_DIR="${{ github.event.inputs.site_folder }}"
          if [ -z "$SOURCE_DIR" ]; then
            SOURCE_DIR="."
          fi
          
          echo ""
          echo "📁 جاري نسخ الملفات من: $SOURCE_DIR"
          
          # نسخ جميع الملفات والمجلدات
          if [ "$SOURCE_DIR" = "." ]; then
            # نسخ كل شيء ما عدا المجلدات المستثناة
            shopt -s dotglob
            for item in "$SOURCE_DIR"/*; do
              basename=$(basename "$item")
              if [ "$basename" != ".git" ] && [ "$basename" != ".github" ] && [ "$basename" != "node_modules" ] && [ "$basename" != "android" ] && [ "$basename" != "ios" ] && [ "$basename" != "www" ] && [ "$basename" != "package.json" ] && [ "$basename" != "package-lock.json" ] && [ "$basename" != "capacitor.config.json" ]; then
                echo "  📄 نسخ: $basename"
                cp -r "$item" www/ 2>/dev/null || true
              fi
            done
          else
            # نسخ من مجلد محدد
            if [ -d "$SOURCE_DIR" ]; then
              echo "  ✅ المجلد $SOURCE_DIR موجود"
              cp -r "$SOURCE_DIR"/* www/ 2>/dev/null || true
              cp -r "$SOURCE_DIR"/.* www/ 2>/dev/null || true
            else
              echo "  ❌ المجلد $SOURCE_DIR غير موجود!"
              echo "  سيتم نسخ جميع الملفات من المجلد الرئيسي"
              shopt -s dotglob
              for item in ./*; do
                basename=$(basename "$item")
                if [ "$basename" != ".git" ] && [ "$basename" != ".github" ] && [ "$basename" != "node_modules" ] && [ "$basename" != "android" ] && [ "$basename" != "ios" ] && [ "$basename" != "www" ] && [ "$basename" != "package.json" ] && [ "$basename" != "package-lock.json" ] && [ "$basename" != "capacitor.config.json" ]; then
                  echo "  📄 نسخ: $basename"
                  cp -r "$item" www/ 2>/dev/null || true
                fi
              done
            fi
          fi
          
          # حذف أي ملفات غير مرغوب فيها داخل www
          rm -rf www/.git www/.github www/node_modules www/.gitignore 2>/dev/null || true
          rm -rf www/package.json www/package-lock.json 2>/dev/null || true
          rm -rf www/android www/ios www/capacitor.config.json 2>/dev/null || true
          rm -rf www/icon.png www/icon.jpg www/icon.jpeg 2>/dev/null || true
          
          echo ""
          echo "✅ تم نسخ جميع ملفات الموقع"
          echo ""
          echo "📊 إحصائيات مجلد www:"
          echo "  📁 عدد الملفات: $(find www -type f | wc -l)"
          echo "  📂 عدد المجلدات: $(find www -type d | wc -l)"
          echo "  💾 الحجم الإجمالي: $(du -sh www | cut -f1)"
          
          # التحقق من وجود index.html
          if [ -f "www/index.html" ]; then
            echo ""
            echo "✅ تم العثور على index.html"
          else
            echo ""
            echo "⚠️ تحذير: لم يتم العثور على index.html!"
            echo "🔍 البحث عن أي ملف HTML في www:"
            find www -name "*.html" -type f
          fi

      - name: 📄 إنشاء package.json
        run: |
          cat > package.json << EOF
          {
            "name": "${{ github.event.inputs.app_name }}",
            "version": "1.0.0",
            "description": "تطبيق ${{ github.event.inputs.app_name }}",
            "main": "www/index.html",
            "scripts": {
              "build": "echo 'Build complete'"
            },
            "dependencies": {
              "@capacitor/android": "^5.0.0",
              "@capacitor/core": "^5.0.0",
              "@capacitor/cli": "^5.0.0",
              "@capacitor/filesystem": "^5.0.0",
              "@capacitor/app": "^5.0.0",
              "@capacitor/status-bar": "^5.0.0",
              "@capacitor/splash-screen": "^5.0.0"
            }
          }
          EOF

      - name: ⚙️ إعداد Capacitor مع Splash Screen
        run: |
          cat > capacitor.config.json << EOF
          {
            "appId": "${{ github.event.inputs.app_id }}",
            "appName": "${{ github.event.inputs.app_name }}",
            "webDir": "www",
            "bundledWebRuntime": false,
            "server": {
              "androidScheme": "http",
              "allowNavigation": ["*"],
              "cleartext": true
            },
            "android": {
              "allowMixedContent": true,
              "captureInput": true,
              "webContentsDebuggingEnabled": true
            },
            "plugins": {
              "SplashScreen": {
                "launchShowDuration": ${{ github.event.inputs.splash_duration }},
                "launchAutoHide": true,
                "launchFadeOutDuration": 500,
                "backgroundColor": "${{ github.event.inputs.app_color }}",
                "androidSplashResourceName": "splash",
                "androidScaleType": "FIT_CENTER",
                "showSpinner": false,
                "splashFullScreen": true,
                "splashImmersive": true
              },
              "StatusBar": {
                "style": "DARK",
                "backgroundColor": "${{ github.event.inputs.app_color }}",
                "overlaysWebView": false
              }
            }
          }
          EOF

      - name: 📦 تثبيت المكتبات
        run: npm install

      - name: 🤖 إنشاء مشروع Android
        run: npx cap add android

      - name: 🎨 إنشاء الأيقونات بجميع المقاسات
        run: |
          ICON_FILE="${{ github.event.inputs.icon_file }}"
          
          # التحقق من وجود ملف الأيقونة
          if [ ! -f "$ICON_FILE" ]; then
            echo "❌ خطأ: ملف الأيقونة $ICON_FILE غير موجود!"
            echo "📁 البحث عن ملفات PNG في المشروع:"
            find . -name "*.png" -not -path './node_modules/*' -not -path './.git/*'
            exit 1
          fi
          
          echo "✅ تم العثور على ملف الأيقونة: $ICON_FILE"
          echo "📏 حجم الصورة الأصلي: $(identify -format '%wx%h' $ICON_FILE)"
          echo ""
          
          # حذف الأيقونات القديمة
          rm -rf android/app/src/main/res/mipmap-*
          rm -rf android/app/src/main/res/drawable/ic_launcher_*
          rm -rf android/app/src/main/res/drawable-*
          
          # إنشاء جميع مجلدات المقاسات
          mkdir -p android/app/src/main/res/mipmap-ldpi
          mkdir -p android/app/src/main/res/mipmap-mdpi
          mkdir -p android/app/src/main/res/mipmap-hdpi
          mkdir -p android/app/src/main/res/mipmap-xhdpi
          mkdir -p android/app/src/main/res/mipmap-xxhdpi
          mkdir -p android/app/src/main/res/mipmap-xxxhdpi
          mkdir -p android/app/src/main/res/mipmap-anydpi-v26
          mkdir -p android/app/src/main/res/drawable
          mkdir -p android/app/src/main/res/drawable-ldpi
          mkdir -p android/app/src/main/res/drawable-mdpi
          mkdir -p android/app/src/main/res/drawable-hdpi
          mkdir -p android/app/src/main/res/drawable-xhdpi
          mkdir -p android/app/src/main/res/drawable-xxhdpi
          mkdir -p android/app/src/main/res/drawable-xxxhdpi
          
          echo "🎨 جاري إنشاء الأيقونات بجميع المقاسات..."
          echo ""
          
          # إنشاء الأيقونات بجميع المقاسات للـ mipmap
          echo "📱 أيقونات mipmap (للتطبيق):"
          convert $ICON_FILE -resize 36x36   android/app/src/main/res/mipmap-ldpi/ic_launcher.png   && echo "  ✅ ldpi:    36x36"
          convert $ICON_FILE -resize 48x48   android/app/src/main/res/mipmap-mdpi/ic_launcher.png   && echo "  ✅ mdpi:    48x48"
          convert $ICON_FILE -resize 72x72   android/app/src/main/res/mipmap-hdpi/ic_launcher.png   && echo "  ✅ hdpi:    72x72"
          convert $ICON_FILE -resize 96x96   android/app/src/main/res/mipmap-xhdpi/ic_launcher.png  && echo "  ✅ xhdpi:   96x96"
          convert $ICON_FILE -resize 144x144 android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png && echo "  ✅ xxhdpi: 144x144"
          convert $ICON_FILE -resize 192x192 android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png && echo "  ✅ xxxhdpi:192x192"
          
          echo ""
          echo "🔧 أيقونات drawable (للإشعارات والأماكن الأخرى):"
          convert $ICON_FILE -resize 18x18   android/app/src/main/res/drawable-ldpi/ic_launcher.png   && echo "  ✅ ldpi:    18x18"
          convert $ICON_FILE -resize 24x24   android/app/src/main/res/drawable-mdpi/ic_launcher.png   && echo "  ✅ mdpi:    24x24"
          convert $ICON_FILE -resize 36x36   android/app/src/main/res/drawable-hdpi/ic_launcher.png   && echo "  ✅ hdpi:    36x36"
          convert $ICON_FILE -resize 48x48   android/app/src/main/res/drawable-xhdpi/ic_launcher.png  && echo "  ✅ xhdpi:   48x48"
          convert $ICON_FILE -resize 72x72   android/app/src/main/res/drawable-xxhdpi/ic_launcher.png && echo "  ✅ xxhdpi:  72x72"
          convert $ICON_FILE -resize 96x96   android/app/src/main/res/drawable-xxxhdpi/ic_launcher.png && echo "  ✅ xxxhdpi: 96x96"
          
          echo ""
          echo "🎯 أيقونات إضافية:"
          # نسخ الصورة الأصلية للـ foreground
          cp $ICON_FILE android/app/src/main/res/drawable/ic_launcher_foreground.png && echo "  ✅ Foreground"
          
          # إنشاء أيقونة مربعة للإشعارات
          convert $ICON_FILE -resize 72x72 -background none -gravity center -extent 72x72 android/app/src/main/res/drawable/ic_launcher_square.png && echo "  ✅ Square icon"
          
          # إنشاء أيقونة دائرية
          convert $ICON_FILE -resize 72x72 \( +clone -threshold -1 -draw 'circle 36,36 36,0' \) -alpha off -compose CopyOpacity -composite android/app/src/main/res/drawable/ic_launcher_round.png && echo "  ✅ Round icon"
          
          echo ""
          echo "✅ تم إنشاء جميع الأيقونات بنجاح!"

      - name: 🎨 إنشاء Splash Screen مخصصة (بدون إطار)
        run: |
          ICON_FILE="${{ github.event.inputs.icon_file }}"
          APP_COLOR="${{ github.event.inputs.app_color }}"
          
          echo "🎨 جاري إنشاء Splash Screen بدون إطار..."
          
          # إنشاء مجلد drawable إذا لم يكن موجوداً
          mkdir -p android/app/src/main/res/drawable
          mkdir -p android/app/src/main/res/values
          
          # إنشاء Splash Screen - صورة كاملة بدون أي إطار أو حدود
          # استخدام خلفية شفافة للأيقونة ودمجها مع لون الخلفية
          convert $ICON_FILE -resize 250x250 -background none -gravity center -extent 300x300 \
                  \( +clone -background "$APP_COLOR" -shadow 0x0+0+0 \) \
                  +swap -background "$APP_COLOR" -layers merge +repage \
                  android/app/src/main/res/drawable/splash.png
          
          echo "  ✅ تم إنشاء splash.png بدون إطار"
          
          # إنشاء خلفية Splash Screen - بسيطة ونظيفة بدون أي إطار
          cat > android/app/src/main/res/drawable/splash_background.xml << EOF
          <?xml version="1.0" encoding="utf-8"?>
          <layer-list xmlns:android="http://schemas.android.com/apk/res/android">
              <item>
                  <shape>
                      <solid android:color="${APP_COLOR}" />
                  </shape>
              </item>
              <item
                  android:gravity="center"
                  android:width="250dp"
                  android:height="250dp">
                  <bitmap
                      android:src="@drawable/splash"
                      android:gravity="center"
                      android:filter="true" />
              </item>
          </layer-list>
          EOF
          echo "  ✅ تم إنشاء splash_background.xml بدون إطار"
          
          # إنشاء ملف styles.xml مع Splash Screen Theme محسن
          cat > android/app/src/main/res/values/styles.xml << EOF
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <!-- السمة الرئيسية للتطبيق -->
              <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
                  <item name="colorPrimary">${APP_COLOR}</item>
                  <item name="colorPrimaryDark">${APP_COLOR}</item>
                  <item name="colorAccent">${APP_COLOR}</item>
                  <item name="android:statusBarColor">${APP_COLOR}</item>
                  <item name="android:navigationBarColor">${APP_COLOR}</item>
                  <item name="android:windowBackground">@color/windowBackground</item>
              </style>
              
              <!-- سمة Splash Screen بدون أي إطارات -->
              <style name="AppTheme.Splash" parent="AppTheme">
                  <item name="android:windowBackground">@drawable/splash_background</item>
                  <item name="android:statusBarColor">${APP_COLOR}</item>
                  <item name="android:navigationBarColor">${APP_COLOR}</item>
                  <item name="android:windowFullscreen">true</item>
                  <item name="android:windowContentOverlay">@null</item>
                  <item name="android:backgroundDimEnabled">false</item>
              </style>
              
              <style name="AppTheme.NoActionBarLaunch" parent="AppTheme.NoActionBar">
                  <item name="android:background">${APP_COLOR}</item>
              </style>
          </resources>
          EOF
          echo "  ✅ تم إنشاء styles.xml محسن"
          
          # إنشاء ملف colors.xml
          cat > android/app/src/main/res/values/colors.xml << EOF
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <color name="windowBackground">${APP_COLOR}</color>
              <color name="primaryColor">${APP_COLOR}</color>
          </resources>
          EOF
          echo "  ✅ تم إنشاء colors.xml"
          
          # إنشاء strings.xml
          cat > android/app/src/main/res/values/strings.xml << EOF
          <?xml version='1.0' encoding='utf-8'?>
          <resources>
              <string name="app_name">${{ github.event.inputs.app_name }}</string>
              <string name="title_activity_main">${{ github.event.inputs.app_name }}</string>
              <string name="package_name">${{ github.event.inputs.app_id }}</string>
          </resources>
          EOF
          echo "  ✅ تم إنشاء strings.xml"
          
          echo ""
          echo "✨ تم إنشاء Splash Screen نظيفة بدون أي إطارات!"

      - name: 🎨 إنشاء الأيقونة التكيفية (لأندرويد 8+)
        run: |
          # إنشاء خلفية الأيقونة
          cat > android/app/src/main/res/drawable/ic_launcher_background.xml << EOF
          <?xml version="1.0" encoding="utf-8"?>
          <shape xmlns:android="http://schemas.android.com/apk/res/android">
              <solid android:color="${{ github.event.inputs.app_color }}"/>
          </shape>
          EOF
          
          # إنشاء ملف الأيقونة التكيفية الرئيسية
          cat > android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
              <background android:drawable="@drawable/ic_launcher_background"/>
              <foreground android:drawable="@drawable/ic_launcher_foreground"/>
          </adaptive-icon>
          EOF
          
          # إنشاء الأيقونة الدائرية التكيفية
          cat > android/app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
              <background android:drawable="@drawable/ic_launcher_background"/>
              <foreground android:drawable="@drawable/ic_launcher_foreground"/>
          </adaptive-icon>
          EOF
          
          echo "✅ تم إنشاء الأيقونة التكيفية لأندرويد 8+"

      - name: 🔧 إعداد ملف AndroidManifest.xml مع Splash Screen
        run: |
          MANIFEST_FILE="android/app/src/main/AndroidManifest.xml"
          
          if [ -f "$MANIFEST_FILE" ]; then
            echo "📝 جاري تعديل AndroidManifest.xml..."
            
            # إنشاء نسخة احتياطية
            cp $MANIFEST_FILE ${MANIFEST_FILE}.backup
            
            # إزالة أي إعدادات قديمة
            sed -i 's/android:networkSecurityConfig="[^"]*"//g' $MANIFEST_FILE
            sed -i 's/android:usesCleartextTraffic="[^"]*"//g' $MANIFEST_FILE
            sed -i 's/android:hardwareAccelerated="[^"]*"//g' $MANIFEST_FILE
            sed -i 's/android:theme="[^"]*"//g' $MANIFEST_FILE
            
            # تعديل وسم application مع Theme Splash
            sed -i 's|<application|<application android:usesCleartextTraffic="true" android:hardwareAccelerated="true" android:theme="@style/AppTheme.Splash"|' $MANIFEST_FILE
            
            # إضافة uses-permission للأذونات
            if ! grep -q "INTERNET" $MANIFEST_FILE; then
              sed -i 's|</manifest>|    <uses-permission android:name="android.permission.INTERNET"/>\n    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>\n</manifest>|' $MANIFEST_FILE
            fi
            
            echo "📋 محتوى AndroidManifest.xml النهائي:"
            cat $MANIFEST_FILE
            
            echo ""
            echo "✅ تم تعديل AndroidManifest.xml"
          else
            echo "❌ ملف AndroidManifest.xml غير موجود!"
            exit 1
          fi

      - name: 🔄 مزامنة المشروع
        run: |
          echo "🔄 جاري مزامنة الملفات..."
          npx cap sync android
          
          # التحقق من وجود splash
          echo ""
          echo "🔍 التحقق من ملفات Splash Screen:"
          if [ -f "android/app/src/main/res/drawable/splash.png" ]; then
            echo "  ✅ splash.png موجودة"
            echo "  📏 أبعاد الصورة: $(identify -format '%wx%h' android/app/src/main/res/drawable/splash.png)"
          fi
          if [ -f "android/app/src/main/res/drawable/splash_background.xml" ]; then
            echo "  ✅ splash_background.xml موجود"
          fi
          
          # التحقق من assets
          echo ""
          echo "📂 التحقق من ملفات assets:"
          if [ -d "android/app/src/main/assets" ]; then
            echo "  💾 عدد الملفات: $(find android/app/src/main/assets -type f | wc -l)"
            echo "  💾 الحجم: $(du -sh android/app/src/main/assets | cut -f1)"
          fi

      - name: 🔨 بناء تطبيق APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: 📊 معلومات الملف الناتج
        run: |
          APK_PATH="android/app/build/outputs/apk/debug/app-debug.apk"
          if [ -f "$APK_PATH" ]; then
            echo "✅ تم بناء APK بنجاح"
            echo "📱 حجم الملف: $(du -h $APK_PATH | cut -f1)"
            echo "📋 اسم التطبيق: ${{ github.event.inputs.app_name }}"
            echo "🆔 App ID: ${{ github.event.inputs.app_id }}"
            echo "🎨 لون الخلفية: ${{ github.event.inputs.app_color }}"
            echo "⏱️ مدة Splash: ${{ github.event.inputs.splash_duration }} مللي ثانية"
            echo ""
            echo "🎨 تم بناء Splash Screen بدون إطارات ✨"
            echo ""
            echo "📦 محتويات APK من ملفات splash:"
            unzip -l $APK_PATH | grep -E "splash|ic_launcher|colors" || echo "  جاري الفحص..."
          else
            echo "❌ فشل بناء APK"
            exit 1
          fi

      - name: 📤 رفع ملف APK
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.inputs.app_name }}-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 90

      - name: 🎉 اكتمال البناء
        run: |
          echo "╔════════════════════════════════════════╗"
          echo "║  🎊 تم بناء التطبيق بنجاح!           ║"
          echo "║  ✨ Splash Screen بدون إطارات        ║"
          echo "╚════════════════════════════════════════╝"
          echo ""
          echo "📱 اسم التطبيق: ${{ github.event.inputs.app_name }}"
          echo "🎨 لون الخلفية: ${{ github.event.inputs.app_color }}"
          echo "⏱️ مدة شاشة البداية: ${{ github.event.inputs.splash_duration }}ms"
          echo ""
          echo "✨ المميزات:"
          echo "  🎨 شاشة بداية بدون أي إطارات أو حدود"
          echo "  🖼️ أيقونة في المنتصف بخلفية موحدة"
          echo "  📱 أيقونات بجميع المقاسات"
          echo "  🎯 أيقونة تكيفية لأندرويد 8+"
          echo "  ⭕ أيقونة دائرية للشاشات المستديرة"
          echo "  🎭 شريط حالة مخصص بنفس لون التطبيق"
          echo ""
          echo "📥 يمكنك تحميل الـ APK من قسم Artifacts"
